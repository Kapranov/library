import images from 'library/utils/images';
import { module, test } from 'qunit';

module('Unit | Utility | images');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = images();
  assert.ok(result);
});
