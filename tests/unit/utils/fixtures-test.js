import fixtures from 'library/utils/fixtures';
import { module, test } from 'qunit';

module('Unit | Utility | fixtures');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fixtures();
  assert.ok(result);
});
