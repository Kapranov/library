import makeModel from 'library/utils/make-model';
import { module, test } from 'qunit';

module('Unit | Utility | make model');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeModel();
  assert.ok(result);
});
