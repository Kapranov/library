import { colorBlock } from 'library/helpers/color-block';
import { module, test } from 'qunit';

module('Unit | Helper | color block');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = colorBlock([42]);
  assert.ok(result);
});
