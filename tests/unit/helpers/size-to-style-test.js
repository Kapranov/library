import { sizeToStyle } from 'library/helpers/size-to-style';
import { module, test } from 'qunit';

module('Unit | Helper | size to style');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sizeToStyle([42]);
  assert.ok(result);
});
