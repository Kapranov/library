import { pageState } from 'library/helpers/page-state';
import { module, test } from 'qunit';

module('Unit | Helper | page state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pageState([42]);
  assert.ok(result);
});
